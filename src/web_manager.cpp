#include "web_manager.h"
#include "app_config.h"
#include "battery_service.h"
#include "config_service.h"
#include "home_assistant.h"
#include "network_service.h"
#include <ArduinoJson.h>
#include <ESPmDNS.h>
#include <WebServer.h>
#include <WiFi.h>

namespace {
WebServer g_server(WEB_MANAGER_PORT);bool g_routes_registered=false,g_mdns_started=false;uint32_t g_last_mdns_attempt_ms=0,g_reboot_at_ms=0;
static const char INDEX_HTML[] PROGMEM=R"HTML(<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Home Control Panel</title><style>:root{color-scheme:dark;font-family:system-ui,-apple-system,sans-serif}*{box-sizing:border-box}body{margin:0;background:#0f172a;color:#e5e7eb}.wrap{max-width:1000px;margin:auto;padding:24px}.card{background:#172033;border:1px solid #334155;border-radius:14px;padding:18px;margin-bottom:16px}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:12px}.stat{background:#111827;border-radius:10px;padding:12px}.k{color:#94a3b8;font-size:.82rem}.v{margin-top:4px;font-weight:650;word-break:break-word}label{display:block;margin:.75rem 0 .3rem;color:#cbd5e1}input,select,button{font:inherit;border-radius:8px;border:1px solid #475569;padding:10px;background:#0f172a;color:#f8fafc}input,select{width:100%}button{background:#2563eb;border:0;cursor:pointer;font-weight:650}.row{display:flex;gap:10px;flex-wrap:wrap;align-items:center}.muted{color:#94a3b8;font-size:.9rem}.result{white-space:pre-wrap;background:#111827;border-radius:8px;padding:10px;margin-top:10px}</style></head><body><div class="wrap"><h1>Home Control Panel <span id="ver" class="muted"></span></h1><div class="card"><h2>Device status</h2><div id="stats" class="grid"></div><div class="row" style="margin-top:14px"><button onclick="testHA()">Test Home Assistant</button><button onclick="reboot()">Reboot</button></div><div id="msg" class="result muted">Ready.</div></div><div class="card"><h2>Panel configuration</h2><form id="cfg"><div class="grid"><div><label>Device ID / hostname</label><input id="device_id" maxlength="31"></div><div><label>Display name</label><input id="display_name" maxlength="47"></div><div><label>Profile</label><select id="profile"><option value="calendar">Calendar</option><option value="room">Room controller</option><option value="whole_home">Whole home</option><option value="custom">Custom</option></select></div><div><label>Home Assistant area ID</label><input id="area_id" maxlength="63" placeholder="living_room"></div><div><label>Modules (comma separated)</label><input id="modules" placeholder="overview,room,media,climate,security,settings"></div><div><label>Backlight</label><input id="backlight" type="number" min="10" max="100"></div><div><label>Screen timeout seconds</label><input id="timeout" type="number" min="0" max="3600"></div></div><h3>Home Assistant</h3><div class="grid"><div><label>Base URL</label><input id="ha_url" placeholder="https://homeassistant.local:8123"></div><div><label>Access token</label><input id="ha_token" type="password" placeholder="Leave blank to keep existing token"></div></div><p class="muted">The token is stored in NVS and is never returned to this page. Changes take effect after reboot.</p><button type="submit">Save configuration</button></form></div></div><script>const $=id=>document.getElementById(id);async function j(url,opt){const r=await fetch(url,opt);let x={};try{x=await r.json()}catch(e){}if(!r.ok)throw new Error(x.error||('HTTP '+r.status));return x}function esc(s){return String(s??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]))}async function status(){try{const s=await j('/api/status');$('ver').textContent='v'+s.version;const p=[['Device',s.device_id],['Profile',s.profile],['Area',s.area||'(none)'],['IP',s.ip],['RSSI',s.rssi+' dBm'],['Battery',s.battery_valid?(s.battery_percent+'% / '+Number(s.battery_voltage).toFixed(3)+' V'):'unavailable'],['Home Assistant',s.ha_message],['Modules',s.modules]];$('stats').innerHTML=p.map(x=>`<div class="stat"><div class="k">${esc(x[0])}</div><div class="v">${esc(x[1])}</div></div>`).join('')}catch(e){}}async function load(){const c=await j('/api/config');for(const id of ['device_id','display_name','profile','area_id','modules','backlight','timeout','ha_url'])$(id).value=c[id]??''}$('cfg').addEventListener('submit',async e=>{e.preventDefault();const p=new URLSearchParams();for(const id of ['device_id','display_name','profile','area_id','modules','backlight','timeout','ha_url','ha_token'])p.set(id,$(id).value);try{const r=await j('/api/config',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:p});$('msg').textContent=r.message;$('ha_token').value=''}catch(e){$('msg').textContent=e.message}});async function testHA(){try{const r=await j('/api/ha/test',{method:'POST'});$('msg').textContent=r.queued?'HA test queued on the single worker.':'HA test could not be queued.'}catch(e){$('msg').textContent=e.message}}async function reboot(){try{await j('/api/reboot',{method:'POST'});$('msg').textContent='Rebooting…'}catch(e){$('msg').textContent=e.message}}status();load().catch(()=>{});setInterval(status,5000);</script></body></html>)HTML";
bool ensure_auth(){if(g_server.authenticate(WEB_MANAGER_USER,WEB_MANAGER_PASSWORD))return true;g_server.requestAuthentication();return false;}void send_json(JsonDocument &doc,int code=200){String p;serializeJson(doc,p);g_server.send(code,"application/json",p);}void send_error(int code,const char *m){JsonDocument d;d["ok"]=false;d["error"]=m;send_json(d,code);}
void handle_status(){if(!ensure_auth())return;const PanelConfig &cfg=config_service_get();HomeAssistantStatus ha={};home_assistant_get_status(ha);BatteryStatus b={};const bool bok=battery_service_get_status(b);JsonDocument d;d["app"]=APP_NAME;d["version"]=APP_VERSION;d["device_id"]=cfg.device_id;d["profile"]=cfg.profile;d["area"]=cfg.area_id;d["modules"]=config_service_modules_csv();d["ip"]=network_service_connected()?WiFi.localIP().toString():String("offline");d["rssi"]=network_service_connected()?WiFi.RSSI():0;d["free_heap"]=ESP.getFreeHeap();d["free_psram"]=ESP.getFreePsram();d["battery_valid"]=bok;d["battery_percent"]=b.percent;d["battery_voltage"]=b.voltage_v;d["ha_configured"]=ha.configured;d["ha_authenticated"]=ha.authenticated;d["ha_message"]=ha.message;d["ha_last_success_ms"]=ha.last_success_ms;send_json(d);}
void handle_get_config(){if(!ensure_auth())return;const PanelConfig &cfg=config_service_get();JsonDocument d;d["device_id"]=cfg.device_id;d["display_name"]=cfg.display_name;d["profile"]=cfg.profile;d["area_id"]=cfg.area_id;d["modules"]=config_service_modules_csv();d["backlight"]=cfg.backlight;d["timeout"]=cfg.screen_timeout_seconds;d["ha_url"]=home_assistant_base_url();d["ha_token_configured"]=home_assistant_token_configured();send_json(d);}
void handle_save_config(){if(!ensure_auth())return;PanelConfig next=config_service_get();String device=g_server.arg("device_id"),name=g_server.arg("display_name"),profile=g_server.arg("profile"),area=g_server.arg("area_id"),modules=g_server.arg("modules");device.trim();name.trim();profile.trim();profile.toLowerCase();area.trim();if(device.isEmpty()||name.isEmpty()){send_error(400,"Device ID and display name are required.");return;}if(profile!="calendar"&&profile!="room"&&profile!="whole_home"&&profile!="custom"){send_error(400,"Invalid profile.");return;}snprintf(next.device_id,sizeof(next.device_id),"%s",device.substring(0,31).c_str());snprintf(next.display_name,sizeof(next.display_name),"%s",name.substring(0,47).c_str());snprintf(next.profile,sizeof(next.profile),"%s",profile.c_str());snprintf(next.area_id,sizeof(next.area_id),"%s",area.substring(0,63).c_str());next.backlight=static_cast<uint8_t>(constrain(g_server.arg("backlight").toInt(),10,100));next.screen_timeout_seconds=static_cast<uint32_t>(constrain(g_server.arg("timeout").toInt(),0,3600));if(!config_service_parse_modules_csv(modules,next))config_service_set_profile_defaults(next);if(!config_service_save(next)){send_error(500,"Could not save panel configuration.");return;}const String hu=g_server.arg("ha_url"),ht=g_server.arg("ha_token");if(!home_assistant_set_credentials(hu.c_str(),ht.c_str())){send_error(400,"Home Assistant URL must begin with http:// or https://.");return;}JsonDocument d;d["ok"]=true;d["reboot_required"]=true;d["message"]="Saved. Reboot the panel to rebuild the module registry from the new profile.";send_json(d);}
void handle_ha_test(){if(!ensure_auth())return;JsonDocument d;d["ok"]=true;d["queued"]=home_assistant_request_health_check();send_json(d);}void handle_reboot(){if(!ensure_auth())return;JsonDocument d;d["ok"]=true;d["rebooting"]=true;send_json(d);g_reboot_at_ms=millis()+700;}
void register_routes(){if(g_routes_registered)return;g_routes_registered=true;g_server.on("/",HTTP_GET,[](){if(!ensure_auth())return;g_server.send_P(200,"text/html; charset=utf-8",INDEX_HTML);});g_server.on("/api/status",HTTP_GET,handle_status);g_server.on("/api/config",HTTP_GET,handle_get_config);g_server.on("/api/config",HTTP_POST,handle_save_config);g_server.on("/api/ha/test",HTTP_POST,handle_ha_test);g_server.on("/api/reboot",HTTP_POST,handle_reboot);g_server.onNotFound([](){if(!ensure_auth())return;send_error(404,"Not found");});}
}
void web_manager_begin(){register_routes();g_server.begin();Serial0.printf("[Web] Management server started on port %u\n",static_cast<unsigned>(WEB_MANAGER_PORT));}
void web_manager_loop(){if(network_service_connected()){const uint32_t now=millis();if(!g_mdns_started&&(!g_last_mdns_attempt_ms||now-g_last_mdns_attempt_ms>=5000UL)){g_last_mdns_attempt_ms=now;if(MDNS.begin(config_service_get().device_id)){MDNS.addService("http","tcp",WEB_MANAGER_PORT);g_mdns_started=true;Serial0.printf("[Web] http://%s.local\n",config_service_get().device_id);}}g_server.handleClient();}else if(g_mdns_started){MDNS.end();g_mdns_started=false;g_last_mdns_attempt_ms=0;}if(g_reboot_at_ms&&static_cast<int32_t>(millis()-g_reboot_at_ms)>=0){Serial0.flush();delay(50);ESP.restart();}}
