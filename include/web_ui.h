#pragma once

// Kept local intentionally: managing the panel must continue to work if the
// household Internet connection is down. This avoids a CDN dependency and
// still fits comfortably in the application partition.
static const char INDEX_HTML[] PROGMEM = R"HTML(<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Home Control Panel</title><style>
:root{color-scheme:dark;font-family:Inter,ui-sans-serif,system-ui,sans-serif;background:#08111f;color:#e8eef9}*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at top right,#183a59,#08111f 48rem);min-height:100vh}.shell{max-width:1180px;margin:auto;padding:22px}.hero{display:flex;justify-content:space-between;align-items:end;gap:16px;margin:8px 0 20px}.hero h1{margin:0;font-size:clamp(1.6rem,4vw,2.35rem)}.muted{color:#9aafc7;font-size:.91rem}.tabs{display:flex;gap:8px;overflow:auto;padding-bottom:10px}.tab,button{font:inherit;color:#e8eef9;background:#17273a;border:1px solid #36516d;border-radius:10px;padding:10px 14px;cursor:pointer;font-weight:650;white-space:nowrap}.tab.active,button.primary{background:#1976d2;border-color:#2b93f5}.tab:hover,button:hover{filter:brightness(1.14)}button:disabled{opacity:.5;cursor:not-allowed}.page{display:none}.page.active{display:block}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(210px,1fr));gap:12px}.card{background:rgba(16,31,49,.94);border:1px solid #2a455f;border-radius:16px;padding:18px;margin:0 0 16px;box-shadow:0 14px 38px #0003}.card h2,.card h3{margin:0 0 8px}.stat,.item{background:#0d1b2d;border:1px solid #263f58;border-radius:11px;padding:12px}.stat b{display:block;margin-top:5px;word-break:break-word}.item{margin-top:10px}.row{display:flex;gap:9px;align-items:center;flex-wrap:wrap}.right{margin-left:auto}.notice{border-left:4px solid #3ca8ff;background:#0b243a;padding:12px;border-radius:8px;margin:12px 0}.ok{color:#7ee7aa}.warn{color:#ffd166}.result{white-space:pre-wrap;min-height:42px}label{display:block;color:#b8c9da;font-size:.86rem;margin:10px 0 4px}input,select{width:100%;font:inherit;background:#091625;color:#eff6ff;border:1px solid #39536e;border-radius:8px;padding:10px}.choice{display:flex;gap:8px;align-items:center}.choice input{width:auto}.entity{display:flex;gap:8px;align-items:center;justify-content:space-between}.pill{font-size:.76rem;padding:3px 7px;border-radius:20px;background:#224766;color:#c8e7ff}.empty{padding:20px;color:#9aafc7;text-align:center}.sticky{position:sticky;bottom:12px;z-index:3}@media(max-width:600px){.shell{padding:13px}.hero{align-items:start;flex-direction:column}.right{margin-left:0}}
</style></head><body><main class="shell"><div class="hero"><div><div class="muted">HOME CONTROL PANEL <span id="ver"></span></div><h1 id="title">Panel display manager</h1></div><div class="muted" id="connection">Loading panel…</div></div>
<nav id="tabs" class="tabs" aria-label="Panel sections"></nav><div id="msg" class="card result muted">Ready.</div>
<section id="overview" class="page"><div class="card"><h2>Overview</h2><p class="muted">Build the panel's start screen from widgets. A widget can occupy one, two, or all four columns.</p><div id="overview_widgets"></div><h3 style="margin-top:18px">Available widgets</h3><div id="widget_catalog" class="row"></div></div><div class="card"><h3>Live panel summary</h3><div id="stats" class="grid"></div></div><div class="card"><div class="notice">Weather and Calendar widgets use Home Assistant entities in this panel's configured area. Save the layout, then scan the area if they do not appear yet.</div><button class="primary" onclick="tab('room')">Configure room controls</button></div></section>
<section id="room" class="page"><div class="card"><div class="row"><div><h2>Room controls</h2><p class="muted">Choose exactly what the Room tab can control. Reorder tiles and mark up to six as favorites.</p></div><button class="right" onclick="scan()">Scan Home Assistant area</button></div><div id="scan_state" class="notice muted">Use Scan to load available entities for the configured area.</div><input id="filter" oninput="renderCandidates()" placeholder="Filter by name, entity ID, or type"><div id="candidates"></div></div><div class="card"><h3>Selected room controls</h3><div id="room_fields"></div><div id="room_hint" class="muted"></div></div></section>
<section id="media" class="page"><div class="card"><div class="row"><div><h2>Media players</h2><p class="muted">Select the players shown by the Media tab. Scanning Home Assistant identifies media players automatically.</p></div><button type="button" class="right" onclick="scan()">Scan Home Assistant area</button></div><div id="media_players"></div></div><div class="card"><h3>One-touch media shortcuts</h3><p class="muted">Optional actions shown above Browse favorites. All four fields are required for a shortcut.</p><div id="shortcut_fields"></div></div></section>
<section id="rooms" class="page"><div class="card"><div class="row"><div><h2>Rooms</h2><p class="muted">Manage the direct controls this panel exposes for its configured Home Assistant area.</p></div><button type="button" class="right" onclick="scan()">Scan Home Assistant area</button></div><div id="rooms_summary" class="notice">Loading selected controls…</div><div class="row"><button type="button" class="primary" onclick="tab('room')">Edit room controls</button><button type="button" onclick="tab('overview')">Edit overview widgets</button></div></div></section><section id="calendar" class="page"><div class="card"><div class="row"><div><h2>Calendar</h2><p class="muted">Add the Calendar widget to the panel Overview, then save. It uses the first Calendar entity in this Home Assistant area.</p></div><button type="button" class="right" onclick="scan()">Scan Home Assistant area</button></div><div id="calendar_summary" class="notice">Loading Calendar widget status…</div><button type="button" class="primary" onclick="ensureWidget('calendar')">Add Calendar widget</button><button type="button" onclick="tab('overview')">Open Overview layout</button></div></section><section id="weather" class="page"><div class="card"><div class="row"><div><h2>Weather</h2><p class="muted">Add the Weather widget to the panel Overview, then save. It uses the first Weather entity in this Home Assistant area.</p></div><button type="button" class="right" onclick="scan()">Scan Home Assistant area</button></div><div id="weather_summary" class="notice">Loading Weather widget status…</div><button type="button" class="primary" onclick="ensureWidget('weather')">Add Weather widget</button><button type="button" onclick="tab('overview')">Open Overview layout</button></div></section><section id="climate" class="page"><div class="card"><h2>Climate</h2><p class="muted">This panel build does not yet expose a configurable climate control adapter. Use Administration to show, hide, or reorder this panel tab.</p></div></section><section id="security" class="page"><div class="card"><h2>Security</h2><p class="muted">This panel build does not yet expose a configurable security control adapter. Use Administration to show, hide, or reorder this panel tab.</p></div></section><section id="settings" class="page"><div class="card"><h2>Settings</h2><p class="muted">System telemetry and diagnostics remain available on the panel itself. Administration provides the configuration controls.</p></div></section>
<section id="administration" class="page"><form id="cfg"><div class="card"><h2>Panel and Home Assistant</h2><div class="grid"><div><label>Device ID / hostname</label><input id="device_id" maxlength="31"></div><div><label>Display name</label><input id="display_name" maxlength="47"></div><div><label>Profile</label><select id="profile"><option value="calendar">Calendar</option><option value="room">Room controller</option><option value="whole_home">Whole home</option><option value="custom">Custom</option></select></div><div><label>Home Assistant area ID</label><input id="area_id" maxlength="63" placeholder="living_room"></div><div><label>Backlight percent</label><input id="backlight" type="number" min="10" max="100"></div><div><label>Screen timeout seconds</label><input id="timeout" type="number" min="0" max="3600"></div><div><label>Home Assistant base URL</label><input id="ha_url" placeholder="https://homeassistant.local:8123"></div><div><label>Access token</label><input id="ha_token" type="password" placeholder="Leave blank to keep the current token"></div></div></div><div class="card"><h3>Panel tab order and visibility</h3><p class="muted">Use the arrows to define the order of the panel navigation. The Overview tab is always retained.</p><div id="module_fields"></div><input id="modules" type="hidden"></div><div class="card"><h3>Service</h3><div class="row"><button type="button" onclick="testHA()">Test Home Assistant</button><button type="button" onclick="reboot()">Reboot panel</button></div><p class="muted">The token is stored in NVS and is never returned. Saving this editor enables the explicit layout and refreshes the live HA subscription.</p></div><div class="sticky card row"><button class="primary" type="submit" disabled>Save layout and configuration</button><span class="muted">Saving applies controls immediately; changing panel tab order requires a reboot.</span></div></form></section></main>
<script>
const $=id=>document.getElementById(id),known=['overview','room','rooms','media','calendar','weather','climate','security','settings'];let cfg={},roomRows=[],candidates=[],players=[],modules=[],overviewWidgets=[],quickActions=[];
const labels={overview:'Overview',room:'Room',rooms:'Rooms',media:'Media',calendar:'Calendar',weather:'Weather',climate:'Climate',security:'Security',settings:'Settings'};async function j(u,o){const r=await fetch(u,o),x=await r.json().catch(()=>({}));if(!r.ok)throw Error(x.error||'HTTP '+r.status);return x}function esc(s){return String(s??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]))}function say(s,kind='muted'){$('msg').className='card result '+kind;$('msg').textContent=s}
function tab(id){if(!$(''+id))id='overview';document.querySelectorAll('.page,.tab').forEach(x=>x.classList.remove('active'));$(id).classList.add('active');document.querySelector(`[data-tab="${id}"]`)?.classList.add('active')}
function nav(){const shown=modules.filter(x=>known.includes(x));$('tabs').innerHTML=[...shown,'administration'].map(x=>`<button class="tab" data-tab="${x}" onclick="tab('${x}')">${esc(labels[x]||'Administration')}</button>`).join('');tab('overview')}
const widgetLabels={home_status:'Home status',lights:'Lights',area:'Area',network:'Network',quick_actions:'Quick actions',weather:'Weather',calendar:'Calendar',panel_tip:'Panel tip'};function widgetStatus(type){return overviewWidgets.some(w=>w.type===type)?'Configured in the Overview layout. Save changes to apply it on the panel.':'Not configured yet.'}function readQuickActions(){quickActions.forEach((a,i)=>{const label=$('quick_label_'+i),type=$('quick_type_'+i),entity=$('quick_entity_'+i);if(label)a.label=label.value;if(type)a.type=type.value;if(entity)a.entity_id=entity.value})}function quickActionEditor(){const options=candidates.filter(e=>['light','switch','fan','cover','scene'].includes(e.domain)).map(e=>`<option value="${esc(e.entity_id)}">${esc(e.name||e.entity_id)}</option>`).join('');return `<div class="notice"><b>Configured buttons</b><p class="muted">Each button is shown on the panel. All Lights needs no target; Toggle and Scene use an entity ID from a Home Assistant scan.</p><datalist id="quick_entities">${options}</datalist>${quickActions.map((a,i)=>`<div class="grid"><div><label>Button text</label><input id="quick_label_${i}" maxlength="39" value="${esc(a.label)}"></div><div><label>Action</label><select id="quick_type_${i}"><option value="all_lights" ${a.type==='all_lights'?'selected':''}>All Lights</option><option value="toggle" ${a.type==='toggle'?'selected':''}>Toggle entity</option><option value="scene" ${a.type==='scene'?'selected':''}>Activate scene</option></select></div><div><label>Target entity</label><input id="quick_entity_${i}" list="quick_entities" maxlength="95" value="${esc(a.entity_id)}" placeholder="Not needed for All Lights"></div><div class="row"><button type="button" onclick="moveQuickAction(${i},-1)">Up</button><button type="button" onclick="moveQuickAction(${i},1)">Down</button><button type="button" onclick="removeQuickAction(${i})">Remove</button></div></div>`).join('')}<button type="button" ${quickActions.length>=6?'disabled':''} onclick="addQuickAction()">Add button</button></div>`}function renderLinkedPages(){if($('rooms_summary'))$('rooms_summary').textContent=roomRows.length?`${roomRows.length} direct room control${roomRows.length===1?'':'s'} selected. Use Edit room controls to reorder, favorite, hide, or remove them.`:'No direct controls selected yet. Scan Home Assistant and add the controls you want.';if($('weather_summary'))$('weather_summary').textContent=widgetStatus('weather');if($('calendar_summary'))$('calendar_summary').textContent=widgetStatus('calendar')}function renderWidgets(){readQuickActions();$('overview_widgets').innerHTML=overviewWidgets.length?overviewWidgets.map((w,i)=>`<div class="item"><div class="row"><b>${esc(widgetLabels[w.type])}</b><span class="pill">${esc(w.type)}</span><button type="button" class="right" onclick="moveWidget(${i},-1)">Up</button><button type="button" onclick="moveWidget(${i},1)">Down</button><button type="button" onclick="removeWidget(${i})">Remove</button></div><div class="grid"><div><label>Display width</label>${w.type==='quick_actions'?'<input value="Full width · 4 columns" disabled>':`<select id="widget_span_${i}" onchange="overviewWidgets[${i}].span=Number(this.value)"><option value="1" ${w.span===1?'selected':''}>Small · 1 column</option><option value="2" ${w.span===2?'selected':''}>Wide · 2 columns</option><option value="4" ${w.span===4?'selected':''}>Full width · 4 columns</option></select>`}</div><div><label>Display height</label><select id="widget_height_${i}" onchange="overviewWidgets[${i}].height=Number(this.value)">${[1,2,3,4].filter(h=>w.type!=='quick_actions'||h>=2).map(h=>`<option value="${h}" ${w.height===h?'selected':''}>${h} row${h===1?'':'s'}</option>`).join('')}</select></div></div>${w.type==='quick_actions'?quickActionEditor():''}</div>`).join(''):'<div class="empty">No widgets selected. Add at least one widget below.</div>';$('widget_catalog').innerHTML=Object.keys(widgetLabels).filter(type=>!overviewWidgets.some(w=>w.type===type)).map(type=>`<button type="button" onclick="addWidget('${type}')">Add ${widgetLabels[type]}</button>`).join(' ');renderLinkedPages()}function addWidget(type){if(overviewWidgets.length>=8||overviewWidgets.some(w=>w.type===type))return;overviewWidgets.push({type,span:(type==='quick_actions'||type==='panel_tip')?4:2,height:type==='quick_actions'?2:1});renderWidgets()}function ensureWidget(type){if(!overviewWidgets.some(w=>w.type===type))addWidget(type);renderLinkedPages();say(widgetLabels[type]+' widget is ready. Save layout and configuration to apply it on the panel.','ok')}function removeWidget(i){readQuickActions();overviewWidgets.splice(i,1);renderWidgets()}function moveWidget(i,d){readQuickActions();const n=i+d;if(n<0||n>=overviewWidgets.length)return;[overviewWidgets[i],overviewWidgets[n]]=[overviewWidgets[n],overviewWidgets[i]];renderWidgets()}function addQuickAction(){readQuickActions();if(quickActions.length<6)quickActions.push({label:'New action',type:'toggle',entity_id:''});renderWidgets()}function removeQuickAction(i){readQuickActions();quickActions.splice(i,1);renderWidgets()}function moveQuickAction(i,d){readQuickActions();const n=i+d;if(n<0||n>=quickActions.length)return;[quickActions[i],quickActions[n]]=[quickActions[n],quickActions[i]];renderWidgets()}
function readRoom(){roomRows.forEach((r,i)=>{r.label=$('room_label_'+i)?.value||'';r.placement=Number($('room_place_'+i)?.value||0)})}function renderRoom(){$('room_fields').innerHTML=roomRows.length?roomRows.map((r,i)=>`<div class="item"><div class="entity"><b>${esc(r.name||r.entity_id)}</b><span class="pill">${esc(r.domain||r.entity_id.split('.')[0])}</span></div><div class="muted">${esc(r.entity_id)}</div><div class="grid"><div><label>Display name</label><input id="room_label_${i}" maxlength="63" value="${esc(r.label)}" placeholder="Use Home Assistant name"></div><div><label>Placement</label><select id="room_place_${i}">${['Grouped','Favorite','Hidden'].map((n,v)=>`<option value="${v}" ${r.placement===v?'selected':''}>${n}</option>`).join('')}</select></div></div><div class="row"><button type="button" onclick="moveRoom(${i},-1)">Up</button><button type="button" onclick="moveRoom(${i},1)">Down</button><button type="button" onclick="removeRoom(${i})">Remove</button></div></div>`).join(''):'<div class="empty">No room controls selected. Scan Home Assistant, then add the controls you want on the panel.</div>';$('room_hint').textContent=roomRows.filter(x=>x.placement===1).length+'/6 favorites selected.';renderLinkedPages()}function moveRoom(i,d){readRoom();const n=i+d;if(n<0||n>=roomRows.length)return;[roomRows[i],roomRows[n]]=[roomRows[n],roomRows[i]];renderRoom()}function removeRoom(i){readRoom();roomRows.splice(i,1);renderRoom()}function addRoom(id){readRoom();const e=candidates.find(x=>x.entity_id===id);if(e&&!roomRows.some(x=>x.entity_id===id))roomRows.push({...e,label:'',placement:0});renderRoom();renderCandidates()}
function renderCandidates(){const q=$('filter').value.toLowerCase(),shown=candidates.filter(e=>(e.name+' '+e.entity_id+' '+e.domain).toLowerCase().includes(q)&&e.domain!=='media_player');$('candidates').innerHTML=shown.length?shown.map(e=>`<div class="item entity"><div><b>${esc(e.name||e.entity_id)}</b><div class="muted">${esc(e.entity_id)}</div></div><span class="pill">${esc(e.domain)}</span><button type="button" ${roomRows.some(r=>r.entity_id===e.entity_id)?'disabled':''} onclick="addRoom('${esc(e.entity_id)}')">Add</button></div>`).join(''):'<div class="empty">No matching controllable entities.</div>'}
function renderPlayers(){$('media_players').innerHTML=players.length?players.map((id,i)=>`<div class="item row"><b>${esc(id)}</b><button type="button" class="right" onclick="players.splice(${i},1);renderPlayers()">Remove</button></div>`).join(''):'<div class="empty">No media players selected. Scan Home Assistant then add one below.</div>';const available=candidates.filter(e=>e.domain==='media_player'&&!players.includes(e.entity_id));$('media_players').innerHTML+=available.map(e=>`<div class="item entity"><span>${esc(e.name||e.entity_id)}<div class="muted">${esc(e.entity_id)}</div></span><button type="button" onclick="players.push('${esc(e.entity_id)}');renderPlayers()">Add player</button></div>`).join('')}
function shortcut(i,s={}){return `<div class="item"><b>Shortcut ${i+1}</b><div class="grid"><div><label>Label</label><input id="shortcut_label_${i}" maxlength="39" value="${esc(s.label)}"></div><div><label>Player entity</label><input id="shortcut_entity_${i}" maxlength="95" value="${esc(s.entity_id)}" placeholder="media_player.office_echo_studio"></div><div><label>Media content ID</label><input id="shortcut_id_${i}" maxlength="191" value="${esc(s.media_content_id)}"></div><div><label>Content type</label><input id="shortcut_type_${i}" maxlength="47" value="${esc(s.media_content_type)}" placeholder="AMAZON_MUSIC"></div></div></div>`}function showModule(id){if(!known.includes(id))return;if(!modules.includes(id))modules.push(id);renderModules();nav();tab(id);say(labels[id]+' is enabled for this layout. Save to apply it on the panel.','ok')}function renderModules(){$('module_fields').innerHTML=modules.map((m,i)=>`<div class="item row"><b>${esc(labels[m]||m)}</b><span class="muted">${esc(m)}</span><button type="button" class="right" onclick="moduleMove(${i},-1)">Up</button><button type="button" onclick="moduleMove(${i},1)">Down</button><button type="button" ${m==='overview'?'disabled':''} onclick="modules.splice(${i},1);renderModules();nav()">Hide</button></div>`).join('')+known.filter(m=>!modules.includes(m)).map(m=>`<button type="button" onclick="showModule('${m}')">Show ${labels[m]}</button>`).join(' ');$('modules').value=modules.join(',')}function moduleMove(i,d){const n=i+d;if(n<0||n>=modules.length)return;[modules[i],modules[n]]=[modules[n],modules[i]];renderModules();nav()}
async function scan(){say('Scanning Home Assistant area. This can take a few seconds…');const r=await j('/api/ha/discover',{method:'POST'});if(!r.queued){say('The scan could not be queued. Check Home Assistant credentials.','warn');return}let tries=0;const poll=async()=>{const x=await j('/api/ha/entities');$('scan_state').textContent=x.message||'Scanning…';if(x.complete||++tries>12){candidates=x.entities||[];roomRows=roomRows.map(r=>({...candidates.find(e=>e.entity_id===r.entity_id),...r}));renderRoom();renderCandidates();renderPlayers();say(candidates.length?'Home Assistant scan complete. Add only the controls you want.':'Scan finished with no supported entities.','ok');return}setTimeout(poll,1200)};poll()}
async function status(){try{const s=await j('/api/status');$('ver').textContent='v'+s.version;$('title').textContent=cfg.display_name||'Panel display manager';$('connection').textContent=s.ip+' · '+(s.ha_authenticated?'Home Assistant connected':'Home Assistant needs attention');const p=[['Device',s.device_id],['Area',s.area||'not set'],['Network',s.ip+' · '+s.rssi+' dBm'],['Memory',Math.round(s.free_heap/1024)+' KB heap · '+Math.round(s.free_psram/1024)+' KB PSRAM'],['Home Assistant',s.ha_message],['Battery',s.battery_valid?s.battery_percent+'% / '+Number(s.battery_voltage).toFixed(3)+' V':'unavailable']];$('stats').innerHTML=p.map(x=>`<div class="stat"><span class="muted">${esc(x[0])}</span><b>${esc(x[1])}</b></div>`).join('')}catch(e){}}
async function testHA(){try{const r=await j('/api/ha/test',{method:'POST'});say(r.queued?'Home Assistant health check queued.':'Health check could not be queued.',r.queued?'ok':'warn')}catch(e){say(e.message,'warn')}}async function reboot(){await j('/api/reboot',{method:'POST'});say('Panel rebooting…','ok')}
async function load(){cfg=await j('/api/config');for(const id of ['device_id','display_name','profile','area_id','backlight','timeout','ha_url'])$(id).value=cfg[id]??'';modules=(cfg.modules||'overview,room,media,climate,security,settings').split(',').filter(Boolean);if(!modules.includes('overview'))modules.unshift('overview');overviewWidgets=(cfg.overview_widgets||[]).map(w=>({...w,height:Number(w.height)||1}));quickActions=(cfg.overview_quick_actions||[]).map(a=>({...a}));roomRows=(cfg.room_controls||[]).map(r=>({...r}));players=cfg.media_players||[];$('shortcut_fields').innerHTML=Array.from({length:3},(_,i)=>shortcut(i,(cfg.media_shortcuts||[])[i]||{})).join('');renderWidgets();renderRoom();renderCandidates();renderPlayers();renderModules();nav();$('cfg').querySelector('button[type=submit]').disabled=false;if(cfg.explicit_layout)$('scan_state').textContent='This panel uses an explicit layout. Scan only when you want to add or change controls.';else $('scan_state').textContent='Legacy automatic discovery is active until you save this new layout.'}
$('cfg').addEventListener('submit',async e=>{e.preventDefault();readRoom();readQuickActions();renderModules();if(!overviewWidgets.length){say('Add at least one Overview widget before saving.','warn');return}const p=new URLSearchParams();for(const id of ['device_id','display_name','profile','area_id','modules','backlight','timeout','ha_url','ha_token'])p.set(id,$(id).value);p.set('overview_widgets',JSON.stringify(overviewWidgets));p.set('overview_quick_actions',JSON.stringify(quickActions));p.set('room_controls',JSON.stringify(roomRows.map(({entity_id,label,placement})=>({entity_id,label,placement}))));p.set('media_players',JSON.stringify(players));for(let i=0;i<3;i++)for(const n of ['label','entity','id','type'])p.set(`shortcut_${n}_${i}`,$(`shortcut_${n}_${i}`).value);try{const r=await j('/api/config',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:p});$('ha_token').value='';say(r.message,'ok')}catch(x){say(x.message,'warn')}});status();load().catch(e=>say('Could not load configuration: '+e.message,'warn'));setInterval(status,5000);
</script></body></html>)HTML";
